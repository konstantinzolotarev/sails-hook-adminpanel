<div class="widget">
    <paper-checkbox name="<%= key %>"
        value="1"
        <%= value && 'checked' %>
        <%= field.config.required && 'required'%>
        <%= field.config.disabled && 'disabled'%>>

        <%= field.config.title %>
    </paper-checkbox>
</div>
